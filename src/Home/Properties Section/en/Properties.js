import React from "react";
import "../Properties.css";
import Icons from "../../../icons"; // Import the Icons object from icons.js

const PropertyItem = ({ iconSrc, labelText, numText }) => {
  return (
   <div></div>
  );
};

const Properties = () => {
  return (
    <div></div>
  );
};

export default Properties;
